//
//  ModuleProtocol.swift
//  Blinken
//
//  Shared contract for all Blinken modules — the seam along which future
//  suite modules (v1.1+) slot in (PRD §2.5).
//

import AppKit

// TODO: Define `protocol BlinkenModule: AnyObject` (identifier, displayName,
//       isEnabled, requiredPermissions, start()/stop(), menuBarView, menuItems())
//       and `enum SystemPermission { case accessibility, inputMonitoring, fullDiskAccess }`.
