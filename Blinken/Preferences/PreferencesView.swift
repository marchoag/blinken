//
//  PreferencesView.swift
//  Blinken
//
//  SwiftUI Settings scene: General (launch at login, show swap bar, LED
//  brightness ceiling) and About (version, links, license) (PRD §1.5).
//

import SwiftUI

struct PreferencesView: View {
    var body: some View {
        // TODO: General + About panes — launch-at-login, swap-bar toggle,
        //       LED brightness ceiling, version/links (PRD §1.5).
        Text("Blinken — Preferences (placeholder)")
            .frame(width: 360, height: 160)
    }
}
